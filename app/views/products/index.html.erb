<h1>Products</h1>

# product get By ID 
<% if @product %>
  <p><%= @product.name %>
  <%=button_to "delete me",
  product_path(@product),
  method: :delete,
  Data:{confirm:"are you sure "}
  %>
  <%=button_to "submit"
  
  %>
  <%=button_to "help"%>
  </p>

<% else %>
  <% @products.each do |product| %>
    <p><%= product.name %>
    <%=button_to "delete ",
    product_path(product),
    method: :delete,
    data:{confirm:"are you sure?"}
    %>
    </p>
  <% end %>

<% end %>

